{
  "name": "keysocket-v2",
  "version": "1.0.0",
  "description": "Modern web-based SSH terminal application",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "npm run build:server && npm run build:client",
    "build:server": "echo 'Server is ready'",
    "build:client": "echo 'Client is ready'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-static-gzip": "^2.1.8",
    "ws": "^8.14.2",
    "ssh2": "^1.14.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
