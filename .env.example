# Production environment for KeySocket

HOST=0.0.0.0
PORT=3000
NODE_ENV=production

# TLS: if you prefer Node to terminate TLS, set USE_TLS=true and provide paths
USE_TLS=false
TLS_KEY=/etc/letsencrypt/live/keysocket.eu/privkey.pem
TLS_CERT=/etc/letsencrypt/live/keysocket.eu/fullchain.pem

# Turnstile (server-side secret)
# Replace with your real secret. This file is ignored by git by default in this repo.
TURNSTILE_SECRET=
TURNSTILE_SITEKEY=
# Note: These both appear to be in sitekey format. Verify with Cloudflare dashboard that TURNSTILE_SECRET is the actual secret key.
TURNSTILE_TOKEN_TTL_MS=30000

# Security / limits
RATE_LIMIT=120
CONCURRENT_PER_IP=5
MAX_PRIVATEKEY_SIZE=65536
# OPTIONAL: comma-separated hosts allowed (restrict destination hosts)
ALLOWED_HOSTS=